// .devcontainer/devcontainer.json
{
    "name": "scroll-dev",
    "runArgs": [
        "--hostname",
        "dev"
    ],
    "build": {
        "dockerfile": "Dockerfile"
    },
    "customizations": {
        "vscode": {
            "extensions": []
        }
    },
    "forwardPorts": [
        3000,
        8080
    ] // Example ports to forward
}